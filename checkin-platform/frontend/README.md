# frontend/ — SPA (esqueleto)

Aún no hay código: esta estructura es un boceto y se detalla al arrancar la
fase F2 (ver `../docs/05-roadmap.md`). Stack propuesto en
`../docs/03-arquitectura.md`.

    src/
      pages/       Vistas: portal alumno (/c/:code) y dashboard instructor
      components/  Piezas compartidas (formularios, tarjetas de equipo, tabs)
      api/         Cliente HTTP hacia /api/v1
      styles/      CSS plano (heredar la estética del portal actual)
